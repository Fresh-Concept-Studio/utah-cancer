<dialog id="coming-soon-modal" class="coming-soon" aria-labelledby="coming-soon-title" aria-describedby="coming-soon-description">
  <div class="coming-soon__content">
    <button type="button" class="coming-soon__close" data-close-coming-soon aria-label="Close coming soon message" autofocus>
      <svg viewBox="0 0 24 24" width="24" height="24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true"><path d="m6 6 12 12M18 6 6 18" /></svg>
    </button>
    <span class="coming-soon__symbol material-symbols-rounded" aria-hidden="true">volunteer_activism</span>
    <p class="coming-soon__foundation">Utah Cancer Foundation</p>
    <h2 id="coming-soon-title">Coming soon.</h2>
    <p id="coming-soon-description">A new way to support our cancer community is on its way. We’re preparing our foundation’s online giving experience. Please check back soon.</p>
    <button type="button" class="coming-soon__back" data-close-coming-soon>Back to the site</button>
  </div>
</dialog>

<style>
  :global(html) { scrollbar-gutter: stable; }
  .coming-soon {
    position: fixed;
    inset: 0;
    margin: auto;
    padding: 0;
    width: min(560px, calc(100% - 32px));
    max-height: calc(100dvh - 32px);
    overflow: auto;
    overscroll-behavior: contain;
    border: 0;
    border-radius: var(--radius-card, 12px);
    background: #fff;
    color: #00476b;
    box-shadow: 0 28px 100px rgb(0 20 35 / 30%);
  }
  .coming-soon[open] { animation: coming-soon-enter 380ms cubic-bezier(.16, 1, .3, 1) both; }
  .coming-soon::backdrop {
    background: rgb(5 24 36 / 76%);
    backdrop-filter: blur(5px);
    animation: coming-soon-overlay 280ms ease-out both;
  }
  .coming-soon__content { position: relative; padding: 60px 48px 48px; text-align: center; }
  .coming-soon__close {
    position: absolute;
    top: 14px;
    right: 14px;
    display: grid;
    place-items: center;
    width: 44px;
    height: 44px;
    border: 0;
    border-radius: 50%;
    background: transparent;
    color: #45616f;
    cursor: pointer;
    transition: background 160ms;
  }
  .coming-soon__close:hover { background: #edf3f6; }
  .coming-soon__symbol {
    display: grid;
    place-items: center;
    width: 84px;
    height: 84px;
    margin: 0 auto 28px;
    border-radius: 50%;
    background: #edf3f6;
    font-size: 42px;
  }
  .coming-soon__foundation { margin: 0 0 12px; color: #45616f; font: 500 14px/1.5 var(--font-body); }
  .coming-soon h2 { margin: 0 0 20px; font: 500 clamp(36px, 6vw, 48px)/1.1 var(--font-heading); letter-spacing: -1.8px; }
  #coming-soon-description { max-width: 370px; margin: 0 auto 32px; color: #52616a; font: 400 16px/1.75 var(--font-body); text-wrap: balance; }
  .coming-soon__back {
    padding: 15px 28px;
    min-height: 48px;
    border: 0;
    border-radius: var(--radius-btn, 8px);
    background: #00476b;
    color: #fff;
    font: 500 14px/1.5 var(--font-body);
    cursor: pointer;
    transition: background 160ms;
  }
  .coming-soon__back:hover { background: #003650; }
  .coming-soon button:focus-visible { outline: 2px solid #00476b; outline-offset: 4px; }
  @keyframes coming-soon-enter { from { opacity: 0; transform: translateY(24px) scale(.96); } to { opacity: 1; transform: translateY(0) scale(1); } }
  @keyframes coming-soon-overlay { from { opacity: 0; } to { opacity: 1; } }
  @media (max-width: 480px) {
    .coming-soon__content { padding: 56px 24px 32px; }
    .coming-soon__symbol { width: 72px; height: 72px; margin-bottom: 24px; font-size: 36px; }
    .coming-soon__back { width: 100%; }
  }
  @media (prefers-reduced-motion: reduce) {
    .coming-soon[open], .coming-soon::backdrop { animation: none; }
    .coming-soon button { transition: none; }
  }
</style>
